# Copy to .env.local and fill in values for local development.
# Vite exposes any variable prefixed with VITE_ to the browser bundle.

# Base URL for the PokéBuilder C# API
# Development: your local ASP.NET Core server
# Production: your Railway-hosted API
VITE_API_URL=http://localhost:5276/api
